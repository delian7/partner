<%= link_to("Add New Project", {:action => 'new'}, :class => 'btn btn-primary', :style=>"float:right;") %>
<table class="table" style="width: 95%!important;">
  <tr>
  	<td class='collapsable'></td>
    <td><b>Project Name</td>
    <td class='collapsable'><b>Active?</td></td> <td><b>Course Name</td><td><strong>Group Size</strong></td>
  </tr></b>
	<% @projects.each do |project| %>
	  <tr>
			<td class='collapsable'><%= project.id %></td>
		  <td><%= project.name %></td>
			<td class='collapsable'><%= project.active %></td>
			<td><%= Course.find(project.course_id).course_title %> (<%= project.course_id %>)</td>
			<td class='collabsable'><%= project.group_size %>
	    <td>
	      <%= link_to("Edit", {:action => 'edit', :id => project.id}, :class => 'btn btn-primary')%>
				<%= link_to("Delete", remove_project_path(project.id), :data => { :confirm =>"Are you sure you want to delete this project?"}, :class => 'btn btn-danger')%>
	    </td>
		</tr>
	<% end %>
</table>