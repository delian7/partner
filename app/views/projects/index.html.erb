<div style="font-size:45px; font-weight:bold;">Projects
<%= link_to("Add New Project", {:action => 'new'}, :class => 'btn btn-primary', :style=>"float:right;margin-bottom:10px;") %>
<table class="table">
  <tr>
  	
    <td><b>Project Name</td>
    <td class='collapsable'><b>Course Name</td><td class='collapsable'><strong>Group Size</strong></td><td></td><td></td>
  </tr></b>
	<% @projects.each do |project| %>
	  <tr>
	  	<td><%= project.name %></td>
		<td class='collapsable'><%= Course.find(project.course_id).course_title %>
		<td class='collapsable'><%= project.group_size %></td>
	    <td>
	      <%= link_to("Edit", {:action => 'edit', :id => project.id}, :class => 'btn btn-success')%></td><td>
		  <%= link_to("Delete", remove_project_path(project.id), :data => { :confirm =>"Are you sure you want to delete this project?"}, :class => 'btn btn-danger')%>
	    </td>
		</tr>
	<% end %>
</table>