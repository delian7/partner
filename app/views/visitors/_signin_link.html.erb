<<<<<<< HEAD
		<% if @ucinetid.blank? %>
			<br>
		<% else %>
			<%= form_for(resource, url: user_session_path) do |f| %>
		    <%= f.hidden_field :ucinetid, value: @ucinetid %> 
		    <%= f.submit "<span class=\"glyphicons glyphicons-log-in\"></span>&emsp;&emsp;Login To PartnerUp".html_safe, style: 'color:#fff;background-color:transparent;box-shadow:0px;border:0px;padding-top: 15px;padding-bottom: 15px;line-height: 20px;text-shadow: 0 1px 0 rgba(255,255,255,.25);font-family: "Helvetica", sans-serif;font-size: 17px;padding-right: 15px;'%>
  		<% end %>
  		<% end %></span>
=======
<% if @ucinetid.blank? %>
<%= link_to (request.ip == "127.0.0.1") ? "" : "https://login.uci.edu/" do %>
		<span class="glyphicons glyphicons-log-in"></span> Login
		<% end %>

<% else %>
	<%= form_for(resource, url: user_session_path) do |f| %>
    	<%= f.hidden_field :ucinetid, value: @ucinetid %> 
    	<%= link_to_submit do %>
		<span class="glyphicons glyphicons-log-in"></span> Login
		<% end %>
	<% end %>
<% end %>
>>>>>>> ayoutchanges
