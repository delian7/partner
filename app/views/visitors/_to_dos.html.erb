<div class="panel panel-default">
  <div class="panel-heading">
    <h2 class="panel-title"><span class="glyphicons glyphicons-notes-2"></span>&emsp;&emsp;To-Dos</h2>
  </div>
  <div class="panel-body">



<%@notifications = false%>
<% case current_user.role %>
<% when 0 %>
	<% if current_user.group_relations.empty? %>
	    <span style="font-size:20px; font-weight:normal;">You don't
	    have any requests</span>
	<% else %>
		<% current_user.groups.order(:project_id).uniq.each do |group| %>
				<%= render 'visitors/todos_student', group: group%>
		<% end %>
	<% end %>
<% when 1..3 %>
   		<% current_user.courses.each do |course|%>
		<%= render 'visitors/todos_professor', course: course %>
		<% end %> 
	<% if @notifications == false %>
		<span style="font-size:20px; font-weight:normal;">You don't
	    have any requests</span>
	<% end %>
<% end %>
</div>